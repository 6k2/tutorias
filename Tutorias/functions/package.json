{
  "name": "tutorias-functions",
  "private": true,
  "engines": {
    "node": "^20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "echo 'no lint'",
    "migrate:chat:dry-run": "node scripts/migrateChatConversations.js --dry-run",
    "migrate:chat:apply": "node scripts/migrateChatConversations.js --apply",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

