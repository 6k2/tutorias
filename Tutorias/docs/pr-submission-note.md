# PR submission note

This branch contains the web-first redesign work on top of the previous session/payment commit.

Latest relevant commits:

- `7f67066` — `Redesign frontend with web-first UI system`
- `b69b911` — `Modernize session guards and add mock payment flow`

If a pull request only shows `Modernize session guards and add mock payment flow`, it is pointing at the earlier merged changes and not at the latest web-first redesign commit.
